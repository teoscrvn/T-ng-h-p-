-- ⚡ Bong bóng mở/tắt Script
local ScreenGui = Instance.new("ScreenGui")
local ImageButton = Instance.new("ImageButton")
local UICorner = Instance.new("UICorner")

ScreenGui.Parent = game.CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

ImageButton.Parent = ScreenGui
ImageButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
ImageButton.BorderSizePixel = 0
ImageButton.Position = UDim2.new(0.1, 0, 0.2, 0)
ImageButton.Size = UDim2.new(0, 40, 0, 40)
ImageButton.Draggable = true
ImageButton.Image = "rbxassetid://15059956542" -- ID ảnh bong bóng
UICorner.CornerRadius = UDim.new(1, 10)
UICorner.Parent = ImageButton

ImageButton.MouseButton1Down:Connect(function()
    game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.End, false, game)
end)

-- ⚡ Load Fluent UI
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
repeat task.wait() until game:IsLoaded()

-- ⚡ Tạo Window chính
local Window = Fluent:CreateWindow({
    Title = "🐯 Téo Hub - Fix Lag",
    SubTitle = "Update v2",
    TabWidth = 157,
    Size = UDim2.fromOffset(470, 320),
    Acrylic = false, -- tắt trong suốt để nền ảnh hiển thị rõ
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.End
})

-- ⚡ Nền bằng ảnh Roblox
local Background = Instance.new("ImageLabel")
Background.Size = UDim2.new(1, 0, 1, 0)
Background.Position = UDim2.new(0, 0, 0, 0)
Background.BackgroundTransparency = 1
Background.Image = "rbxassetid://15059956542" -- 👉 đổi ID ảnh Roblox tại đây
Background.ImageTransparency = 0.2
Background.ZIndex = -1
Background.Parent = Window.Main

-- ⚡ Tabs
local Tabs = {
    Main = Window:AddTab({ Title="⚡ Fix Lag" }),
    Game = Window:AddTab({ Title="🎮 Game riêng" }),
    Info = Window:AddTab({ Title="ℹ️ Thông tin" }),
}
-- ⚡ Các mức Fix Lag
Tabs.Main:AddButton({
    Title = "Fix Lag V1 (nhẹ)",
    Description = "Xoá bớt bóng, giảm chi tiết nhẹ",
    Callback = function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level10
        workspace.GlobalShadows = false
        settings().Rendering.EagerBulkExecution = false
    end
})

Tabs.Main:AddButton({
    Title = "Fix Lag V2 (trung bình)",
    Description = "Giảm bóng, xoá chi tiết + giảm anim",
    Callback = function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level8
        workspace.GlobalShadows = false
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Trail") then
                v.Enabled = false
            end
        end
    end
})

Tabs.Main:AddButton({
    Title = "Fix Lag V3 (mạnh)",
    Description = "Xoá hầu hết hiệu ứng, giảm tầm nhìn",
    Callback = function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level6
        game.Lighting.FogStart = 5
        game.Lighting.FogEnd = 50
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then
                v.Enabled = false
            end
        end
    end
})

Tabs.Main:AddButton({
    Title = "Siêu Fix Lag",
    Description = "Tắt hoàn toàn anim + tầm nhìn cực thấp",
    Callback = function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level1
        game.Lighting.FogStart = 5
        game.Lighting.FogEnd = 20
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Explosion") then
                v.Enabled = false
            end
        end
    end
})

-- ⚡ Game riêng (ví dụ 99 Đêm)
if game.PlaceId == 1234567890 then -- 👉 thay ID game 99 Đêm
    Tabs.Game:AddButton({
        Title = "Ẩn cây",
        Callback = function()
            for _,v in pairs(workspace:GetDescendants()) do
                if v:IsA("MeshPart") and v.Name:lower():find("tree") then
                    v.Transparency = 1
                end
            end
        end
    })

    Tabs.Game:AddButton({
        Title = "Ẩn thịt",
        Callback = function()
            for _,v in pairs(workspace:GetDescendants()) do
                if v:IsA("MeshPart") and v.Name:lower():find("meat") then
                    v.Transparency = 1
                end
            end
        end
    })
end

-- ⚡ Tab thông tin
Tabs.Info:AddParagraph({
    Title = "🐯 Téo Hub",
    Content = "Hub fix lag dành cho mọi game.\nUpdate v2 - có hỗ trợ game riêng."
})