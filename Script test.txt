local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
repeat task.wait() until game:IsLoaded()

local Window = Fluent:CreateWindow({
    Title = "Téo 🍎v2 update =}",
    SubTitle = "vip pro💎😍",
    TabWidth = 157,
    Size = UDim2.fromOffset(450, 300),
    Acrylic = true, -- vẫn có hiệu ứng mờ
    Theme = "Dark", -- theme cơ bản (nhưng sẽ bị ảnh nền che)
    MinimizeKey = Enum.KeyCode.End
})

-- 🖼️ Ảnh nền cho menu (thay ID tuỳ ý)
local Background = Instance.new("ImageLabel")
Background.Parent = Window.Window -- gắn vào khung chính của Fluent
Background.Size = UDim2.fromScale(1, 1)
Background.Position = UDim2.new(0, 0, 0, 0)
Background.BackgroundTransparency = 1
Background.Image = "rbxassetid://120546351045897" -- đổi ID này thành ảnh Roblox bạn muốn
Background.ImageTransparency = 0.1 -- độ trong suốt ảnh (0 = rõ nhất, 1 = ẩn luôn)
Background.ZIndex = 0 -- cho nó ra sau cùng

-- Tabs
local Tabs = {
    Main0 = Window:AddTab({ Title="một số kênh🤓" }),
    Main1 = Window:AddTab({ Title="grow a gaden 🌱🌴🌵🌲" }),
    Main2 = Window:AddTab({ Title="bran rót💩" }),
    Main3 = Window:AddTab({ Title="99 đêm🥷" }),
    Main4 = Window:AddTab({ Title="Script Hop" }),
    Main5 = Window:AddTab({ Title="lin tinh" }),
}

-- Ví dụ button
Tabs.Main0:AddButton({
    Title = "tiktok bạn:)",
    Description = "oách xà lách vô cùng",
    Callback = function()
        setclipboard("https://www.tiktok.com/@lm.nguyn4717?_t=ZS-8zueZXpLRxq&_r=1")
    end
})