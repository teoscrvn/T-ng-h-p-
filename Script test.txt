--// UI Library
loadstring(game:HttpGet("https://raw.githubusercontent.com/daucobonhi/Ui-Redz-V2/main/UiREDzV2.lua"))()

--// Main Window
local Window = MakeWindow({
    Hub = {
        Title = "téo 🍎", -- Tên hub
        Animation = "Youtube: téo🍎 scr vn" -- Dòng phụ
    },
    Key = {
        KeySystem = false, -- Không cần key
    }
})

--// Nút thu nhỏ
MinimizeButton({
    Image = "http://www.roblox.com/asset/?id=96392217298536",
    Size = {60, 60},
    Color = Color3.fromRGB(10, 10, 10),
    Corner = true,
    Stroke = false,
    StrokeColor = Color3.fromRGB(255, 0, 0)
})

------ Tab 1
local Tab1o = MakeTab({Name = "99 đêm Fake"})

-- Kill Aura TP toggle
local KillAuraEnabled = false
local Range = 50 -- phạm vi phát hiện NPC

AddButton(Tab1o, {
    Name = "🗡 Kill Aura TP",
    Callback = function()
        KillAuraEnabled = not KillAuraEnabled
        if KillAuraEnabled then
            print("Kill Aura TP BẬT!")
        else
            print("Kill Aura TP TẮT!")
        end
    end
})

-- Loop Kill Aura TP
spawn(function()
    while true do
        if KillAuraEnabled then
            local player = game.Players.LocalPlayer
            local character = player.Character
            if character and character:FindFirstChild("HumanoidRootPart") then
                local npcsFolder = workspace:FindFirstChild("NPCs") -- Thay đúng folder NPC
                if npcsFolder then
                    for _, npc in pairs(npcsFolder:GetChildren()) do
                        if npc:FindFirstChild("Humanoid") and npc:FindFirstChild("HumanoidRootPart") then
                            local npcHRP = npc.HumanoidRootPart
                            local dist = (npcHRP.Position - character.HumanoidRootPart.Position).Magnitude
                            if dist <= Range then
                                -- TP đến NPC
                                character.HumanoidRootPart.CFrame = npcHRP.CFrame * CFrame.new(0, 3, 0)
                                wait(0.1)
                                -- Tấn công NPC
                                local attackEvent = game:GetService("ReplicatedStorage"):FindFirstChild("Events") 
                                                and game.ReplicatedStorage.Events:FindFirstChild("Attack")
                                if attackEvent then
                                    attackEvent:FireServer(npc.Humanoid)
                                end
                                wait(0.2)
                            end
                        end
                    end
                end
            end
        end
        wait(0.1)
    end
end)